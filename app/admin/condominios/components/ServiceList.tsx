function ServiceList({ condominioId }: { condominioId: string }) {
    // Lógica para buscar e exibir os serviços baseados no condominioId

    return (
        <div>
            {/* Renderize os serviços aqui */}
        </div>
    );
}

export default ServiceList;